import React from 'react';
import { CLDVideoLayerProps, SubtitleOption, QualityOption } from './types';
import { SubtitleCue } from './utils/subtitleUtils';
interface CLDVideoLayerState {
    status: any | null;
    isControlsVisible: boolean;
    isSeeking: boolean;
    seekingPosition: number;
    lastSeekPosition: number;
    isSeekingComplete: boolean;
    isLandscape: boolean;
    isFullScreen: boolean;
    previousOrientation: 'portrait' | 'landscape' | null;
    currentPlaybackSpeed: number;
    isSpeedMenuVisible: boolean;
    currentSubtitle: string;
    isSubtitlesMenuVisible: boolean;
    availableSubtitleTracks: SubtitleOption[];
    subtitleCues: SubtitleCue[];
    activeSubtitleText: string | null;
    currentQuality: string;
    isQualityMenuVisible: boolean;
    availableQualityLevels: QualityOption[];
}
export declare class CLDVideoLayer extends React.Component<CLDVideoLayerProps, CLDVideoLayerState> {
    private videoRef;
    private seekbarRef;
    private panResponder;
    private controlsManager;
    private playbackManager;
    private subtitlesManager;
    private qualityManager;
    private orientationManager;
    constructor(props: CLDVideoLayerProps);
    componentDidMount(): void;
    componentDidUpdate(prevProps: CLDVideoLayerProps): void;
    componentWillUnmount(): void;
    handleStatusUpdate: (s: any) => void;
    handlePlayPause: () => Promise<void>;
    handleMuteToggle: () => Promise<void>;
    handlePlaybackSpeedChange: (speed: number) => Promise<void>;
    handleToggleSpeedMenu: () => void;
    handleSubtitleChange: (languageCode: string) => Promise<void>;
    handleToggleSubtitlesMenu: () => void;
    handleQualityChange: (qualityValue: string) => Promise<void>;
    handleToggleQualityMenu: () => void;
    handleShare: () => Promise<void>;
    handleToggleFullScreen: () => Promise<void>;
    getProgress: () => number;
    getCurrentPosition: () => number;
    toggleControls: () => void;
    render(): React.JSX.Element;
}
export {};
//# sourceMappingURL=CLDVideoLayer.d.ts.map