/**
 * WebVTT parsing utilities for subtitle support
 */
export interface SubtitleCue {
    start: number;
    end: number;
    text: string;
}
/**
 * Parse WebVTT file content into subtitle cues
 */
export declare const parseWebVTT: (content: string) => SubtitleCue[];
/**
 * Find active subtitle cue for given time
 */
export declare const findActiveSubtitle: (cues: SubtitleCue[], currentTime: number) => SubtitleCue | null;
/**
 * Fetch and parse subtitle file from URL
 */
export declare const fetchSubtitleFile: (url: string) => Promise<SubtitleCue[]>;
//# sourceMappingURL=subtitleUtils.d.ts.map